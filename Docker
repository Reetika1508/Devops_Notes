Composs file use to launch multiple contianers but on a single node.
Docker swarm is use to manage clusters of docker hosts/nodes/containers.
Docker swarm nodes - Manager and worker nodes
Commands can be run from any manager nodes but it will execute only by leader node.
docker stack in docker swarm is equivalent to docker compose 
Type of networks: Bridge,host,null
Default bridge network allows cont3ainer to talk using IP but not using continers namehence we use custom bridge nw.
None network means contianer in it does not have any IP, it will not communicate to any other container. Just to itslef. It is isolated.
host nw is when container uses the same port as host. Same IP.
The containers which are expossed to external world will be in the ingress/overlay network and all containers will be in the default custom network created when we deploy the containers.
Docker content trust/CoSIGN: Image signing: Developer creates image can sign the image and push to docker hub for authenticity.
Container can run with over CPU utilization but there will be throtelling. But with ov er memory utlization, it will terminate.

